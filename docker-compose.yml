version: '3.9'

services:

  api:
    build: api
    command: uvicorn main:app --host=0.0.0.0 --port=8000 --reload --reload-include *
    ports:
      - 8000:8000
    volumes:
      - ./api/:/app
    depends_on:
      - postgres
